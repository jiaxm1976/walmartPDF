# 候选：建议归档或删除的旧文档清单

以下文件/目录已被识别并处理：


说明：

如果确认不再需要历史副本，我可以根据你的指示执行永久删除（请回复 `delete` 并列出要删除的具体文件）。
 - 已根据你的指示永久删除以下归档文件：
	 - `archived/backend/database/schema_design_v1.sql`
	 - `archived/scripts/db_import_new_structure.py`

	其他历史文档仍保留在 `.claude/archive/`，如需继续清理请列出要删除的文件。