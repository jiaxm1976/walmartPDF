# 手工业务数据导出文件

本文档说明在本仓库中手动完成三项常用操作的调用方式：数据库初始化、数据导入、数据导出。文档面向在 macOS 上使用项目虚拟环境进行调试与小批量导入/导出的开发者。

**前提**
- 已克隆仓库并在项目根目录；示例命令假设当前工作目录为仓库根目录。
- 使用 Python 虚拟环境（仓库建议为 `.venv`）。

**概览**
- 数据库初始化脚本：[scripts/init_database_v2.py](scripts/init_database_v2.py)
- 批量导入脚本：[scripts/batch_import_v2.py](scripts/batch_import_v2.py)
- 导出为 Excel：[scripts/export_data_to_excel.py](scripts/export_data_to_excel.py)
- 调试与单文件解析示例：[scripts/debug_print_parse_pw1.py](scripts/debug_print_parse_pw1.py)、[scripts/debug_left_header.py](scripts/debug_left_header.py)

## 1. 环境准备

在 macOS 下的常用步骤：

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r backend/requirements.txt
```

如果已有 `.venv`，只需激活：

```bash
source .venv/bin/activate
```

（注：部分脚本也能在不激活虚拟环境时直接使用 `./.venv/bin/python <script>` 的方式运行。）

## 2. 数据库初始化

使用项目提供的初始化脚本：

```bash
./.venv/bin/python scripts/init_database_v2.py
```

或者通过直接调用模块函数（在交互或自动化流程中常用）：

```bash
./.venv/bin/python -c "from backend.database.config import init_database; init_database()"
```

初始化成功后，默认 SQLite 数据库文件位于 `backend/data/walmart_pdf_parser.db`。

## 3. 数据导入（批量与单个）

批量导入（默认扫描脚本内部配置的 PDF 目录）：

```bash
./.venv/bin/python scripts/batch_import_v2.py
```

说明：
- 脚本会输出导入进度、创建的 statement id 及各板块统计。
- 如果需要只导入某个目录或单个 PDF，可把 PDF 放到脚本扫描的目录（例如 `20260101账单/`），或临时修改脚本以传入文件名。

若需手动删除某 PDF 的已导入记录（例如重跑导入前清理）：

```bash
sqlite3 backend/data/walmart_pdf_parser.db "DELETE FROM section_data WHERE statement_id IN (SELECT id FROM statements WHERE pdf_name LIKE 'MP_09232025_statement_summary_PW%'); DELETE FROM statements WHERE pdf_name LIKE 'MP_09232025_statement_summary_PW%';"
```

调试单个 PDF（仅解析并打印合并行与结构化数据，用于排查字段识别）：

```bash
./.venv/bin/python scripts/debug_print_parse_pw1.py
./.venv/bin/python scripts/debug_left_header.py
```

## 4. 数据导出（Excel）

使用导出脚本导出数据库数据为 Excel（脚本头部或注释中说明参数）：

```bash
./.venv/bin/python scripts/export_data_to_excel.py
```

常见输出位置与说明：
- 导出脚本默认会写入 `output/` 目录或脚本同目录下的 `exports/`（以脚本注释为准）。
- 导出的 Excel 会包含 statements 与 section_data 的合并视图，列名与含义请参考 `EXPORT_QUICK_REFERENCE.md`。

## 5. 常见问题与排查步骤

- 问题：导入后金额缺少千位（例如 PDF 显示 `$1,868.55`，导入后为 `868.55`）
  - 排查：使用单文件调试脚本查看合并 OCR 行：

```bash
./.venv/bin/python scripts/debug_print_parse_pw1.py
```

  - 检查 `section_data` 中 header 的 JSON：

```bash
sqlite3 backend/data/walmart_pdf_parser.db "SELECT data FROM section_data WHERE section_name='header' AND statement_id=<ID>;"
```

  - 说明：该类问题通常由解析代码中对逗号的错误分割导致（已在解析逻辑中改为先提取单引号包裹的字段，再清理货币符号与千位分隔符）。如需进一步排查，请运行上述调试脚本并将输出粘贴给维护者。

- 问题：某个板块导入失败或字段缺失
  - 排查：在 `scripts/batch_import_v2.py` 的输出中查找该 PDF 的导入日志，或直接运行 `StructuredDataImporter.import_jg_data` 并在插入前打印 `header_dict`（示例脚本 `scripts/debug_left_header.py` 可参考）。

## 6. 运行示例（一步到位）

以下是一个典型的从初始化到导出的一键序列：

```bash
source .venv/bin/activate
pip install -r backend/requirements.txt
./.venv/bin/python scripts/init_database_v2.py
./.venv/bin/python scripts/batch_import_v2.py
./.venv/bin/python scripts/export_data_to_excel.py
```

## 7. 附录：重要文件与说明

- 初始化：[scripts/init_database_v2.py](scripts/init_database_v2.py)
- 批量导入：[scripts/batch_import_v2.py](scripts/batch_import_v2.py)
- 导出：[scripts/export_data_to_excel.py](scripts/export_data_to_excel.py)
- 单文件调试：[scripts/debug_print_parse_pw1.py](scripts/debug_print_parse_pw1.py)、[scripts/debug_left_header.py](scripts/debug_left_header.py)

---

文档生成于 2026-01-07。如需我将该文档提交到仓库（已经创建）并示范一次导入/导出运行，请回复“执行示例”。
