# ============================================================
# 文件: backend/app/api/__init__.py
# 功能: API模块初始化
#
# 这个包用于放置项目的 HTTP API 实现，例如不同版本的路由、
# 认证依赖和请求/响应模型。将来可在这里做包级配置或导入
# 子模块以供上层应用（如 `backend.main`）导入。对于初学者：
# - FastAPI 使用路由器（`APIRouter`）将端点组织到模块/包中；
# - 在 `backend.main` 中会尝试导入 `backend.app.api.v2.api_router`
#   并挂载到 `/api/v2` 前缀上，从而暴露 v2 的所有端点。
#
# 本文件当前只是占位符，说明包的用途和约定。
# ============================================================

